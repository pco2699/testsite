<div class="container">
  <%= form_for @time_slot, :html => { :class => "form-inline" } do |f| %>
  <% if @time_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_slot.errors.count, "error") %> prohibited this time_slot from being saved:</h2>

      <ul>
      <% @time_slot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    From:<%= f.text_field :date_from, { :class => "form-control register_from", :required => "true" } %>&nbsp;
    To:<%= f.text_field :date_to, { :class => "form-control register_to", :required => "true" } %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.submit "Register", :class => "btn btn-default search_btn" %>
    </div>
<% end %>
</div>
